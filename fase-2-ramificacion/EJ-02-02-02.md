---
ejercicio: EJ-02-02-02
fase: 02
nivel: 02
nivel_nombre: Notable (N2)
titulo: Marko mergea su feature a `develop`
---

## EJ-02-02-02 — Marko fusiona su feature a `develop`

> [!info] Situación
> El manual de Samba está listo. Marko lo incorpora a `develop` (no directamente a `main`).

- **🎯 Objetivo real:** practicar el flujo `feature → develop`.
- **🧩 Problema que resuelve:** saltarse `develop` significa subir a `main` cosas que aún no están auditadas.

---

## 📚 Fundamento

> [!info] Antes de empezar: el destino ahora es `develop`
> Con el flujo de dos ramas, una feature terminada **no** se fusiona en `main`, sino en **`develop`** (el taller). Solo más tarde, cuando todo `develop` esté validado, se hará la release a `main`.

> [!abstract] Idea clave: `pull` antes de `merge`
> Antes de fusionar, actualiza la rama destino con `git pull`, por si alguien (Carlos) ha subido cosas mientras tú trabajabas. Fusionar sobre una rama desactualizada es pedir conflictos.

> [!example] Vocabulario de esta práctica
> - **`feature → develop`:** el camino normal de un trabajo terminado.
> - **Higiene de ramas:** borrar la feature (local y remota) tras fusionarla.
> - **`git push origin --delete <rama>`:** borra la rama en el remoto.

---

## 🛠️ Procedimiento

> [!example] Paso 1 — Termina el manual y haz el commit final (en la feature)
> Completa el manual de Samba con los pasos detallados y:
> ```
> git add 02_active_directory/samba_ad_dc.md
> git commit -m "EJ-02-02-02 · Manual Samba completado: instalación + provisión + verificación"
> git push
> ```

> [!example] Paso 2 — Cámbiate a `develop` y actualízala
> ```
> git switch develop
> git pull
> ```

> [!example] Paso 3 — Fusiona la feature y súbela
> ```
> git merge feature/manual-samba
> git push
> ```

> [!example] Paso 4 — Limpia la rama (local y remota)
> ```
> git branch -d feature/manual-samba
> git push origin --delete feature/manual-samba
> ```

---

## 🚩 Errores y verificación

> [!warning] Errores típicos (evítalos)
> | Error | Qué pasa | Cómo evitarlo |
> | :--- | :--- | :--- |
> | Fusionas sin `git pull` antes | Riesgo de conflicto | Actualiza `develop` con `pull` antes del merge. |
> | Fusionas la feature en `main` | Te saltas la validación | El destino es `develop`, no `main`. |
> | Dejas ramas viejas sin borrar | El repo se llena de ramas muertas | Borra la feature tras fusionarla. |

> [!success] ✅ Verificación: ¿está bien hecho?
> - `git branch -a` ya **no** lista `feature/manual-samba`.
> - En `develop` (GitHub), el manual de Samba está completo.

> [!question] Comprueba que lo has entendido
> Responde en tu bitácora:
> 1. ¿En qué rama se fusiona una feature terminada, y por qué no en `main`?
> 2. ¿Por qué se hace `git pull` antes del merge?
> 3. ¿Cómo se borra una rama en el remoto?

---

## ✅ Entregables y cierre

- **Entregable GitHub:** el manual está en `develop`; la rama feature ha desaparecido.
- **Entregable bóveda:** manual completo de Samba.
- **Criterio de éxito:** feature borrada y manual completo en `develop`.

> [!summary] 🎓 Qué has aprendido en este ejercicio
> - El camino `feature → develop` para el trabajo terminado.
> - A **actualizar** la rama destino con `pull` antes de fusionar.
> - **Higiene de ramas**: borrar la feature (local y remota) tras el merge.
>
> **Siguiente:** EJ-02-02-03 — Marko hace una "release": de `develop` a `main`.
