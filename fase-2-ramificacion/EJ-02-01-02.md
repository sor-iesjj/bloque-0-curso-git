---
Ejercicio: EJ-02-01-02
Fase: 02
Nivel: 01
Nivel_nombre: Mínimo (N1)
Título: Marko fusiona su rama experimento a `main`
---

## EJ-02-01-02 — Marko fusiona su rama experimento a `main`

> [!info] Situación
> El experimento ha funcionado y Lucía aprueba la nueva estructura. Marko quiere incorporarla al `main` oficial.

- **🎯 Objetivo real:** aprender `git merge` para incorporar los cambios de una rama a otra.
- **🧩 Problema que resuelve:** sin merge, los cambios buenos se quedan atrapados en la rama de experimento y no llegan a la versión oficial.

---

## 📚 Fundamento

> [!info] Antes de empezar: fusionar = devolver el canal al río
> **Merge** es la operación de **traer los cambios** de una rama a otra. Te sitúas en la rama que quieres actualizar (aquí `main`) y "tiras hacia dentro" de la otra rama. Es el paso que convierte tu experimento aprobado en parte de la versión oficial.

> [!abstract] Idea clave: "Fast-forward"
> Si `main` **no ha cambiado** mientras trabajabas en la rama, Git no necesita crear nada nuevo: solo **avanza el puntero** de `main` hasta tu trabajo. A eso se le llama **fast-forward** (avance rápido). Si `main` sí hubiera cambiado, Git haría un merge con un commit nuevo.

> [!example] Vocabulario de esta práctica
> - **`git merge <rama>`:** trae los cambios de esa rama a la actual.
> - **Fast-forward:** merge sin commit extra (solo avanza el puntero).
> - **`git branch -d`:** borra una rama que ya no necesitas.

---

## 🛠️ Procedimiento
Trabaja dentro de `Manuales/`.

> [!example] Paso 1 — Sitúate en `main`
> ```
> git switch main
> ```

> [!example] Paso 2 — Fusiona la rama de experimento
> ```
> git merge experimento/nueva-estructura
> ```
> Verás el mensaje **"Fast-forward"**.

> [!example] Paso 3 — Comprueba que el cambio ya está en `main`
> Abre el `README.md`: ahora **SÍ** tiene la línea del experimento.

> [!example] Paso 4 — Borra la rama (ya no hace falta)
> ```
> git branch -d experimento/nueva-estructura
> ```

> [!example] Paso 5 — Sube `main` a GitHub
> ```
> git push
> ```

---

## 🚩 Errores y verificación

> [!warning] Errores típicos (evítalos)
> | Error | Qué pasa | Cómo evitarlo |
> | :--- | :--- | :--- |
> | Haces `merge` estando en la rama equivocada | Fusionas al revés | Antes del merge, sitúate en la rama **destino** (`main`) y compruébalo con `git branch`. |
> | Borras la rama con `-D` sin querer | `-D` fuerza el borrado aunque falte fusionar | Usa `-d` (minúscula): solo borra si ya está fusionada. |

> [!success] ✅ Verificación: ¿está bien hecho?
> - `git branch` ya solo muestra `main`.
> - El README en GitHub tiene el cambio.

> [!question] Comprueba que lo has entendido
> Responde en tu bitácora:
> 1. ¿Qué hace `git merge` y en qué rama hay que estar para hacerlo?
> 2. ¿Qué significa "fast-forward"?
> 3. ¿Qué diferencia hay entre `git branch -d` y `-D`?

---

## ✅ Entregables y cierre

- **Entregable GitHub:** el cambio del README visible en `main`.
- **Entregable bóveda:** README actualizado.
- **Criterio de éxito:** solo queda la rama `main` y el cambio está en GitHub.

> [!summary] 🎓 Qué has aprendido en este ejercicio
> - A **fusionar** una rama en `main` con `git merge`.
> - Qué es un **fast-forward** y cuándo ocurre.
> - A **borrar** una rama ya fusionada de forma segura (`-d`).
>
> **Siguiente:** EJ-02-01-03 — Marko crea una rama *feature* para un manual nuevo.
