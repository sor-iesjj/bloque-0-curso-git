---
Ejercicio: EJ-04-02-05
Fase: 04
Nivel: 02
Nivel_nombre: Notable (N2)
Título: Marko automatiza con un hook commit-msg (validación de formato)
Playlist: B0_Curso_Git
Vídeo: B0.G.4.2.5 · Marko automatiza con un hook commit-msg (validación de formato)
---

## EJ-04-02-05 — Marko automatiza con un hook de validación de mensajes

> [!abstract] Ficha de la práctica
> ### 📌 `B0.G.4.2.5` — Marko automatiza con un hook commit-msg (validación de formato)
> - **Fase 4** (Workflows) · **Nivel 2** (Notable)
> - **🎬 Playlist:** `B0_Curso_Git`
> - **📹 Nombre del vídeo:** `B0.G.4.2.5 · Marko automatiza con un hook commit-msg (validación de formato)`

> [!info] Situación
> Marko quiere asegurarse de que **TODOS** sus commits llevan un mensaje que empieza por `EJ-FF-NN-OO`. Si no, que el commit **falle** automáticamente.

- **🎯 Objetivo real:** configurar un **hook** `commit-msg` que valide el mensaje del commit.
- **🧩 Problema que resuelve:** sin hook, Marko se olvida del prefijo y el profesor no puede verificar los entregables.

---

## 📚 Fundamento

> [!info] Antes de empezar: un hook es un "guardián automático"
> Un **hook** de Git es un pequeño script que se ejecuta **solo**, en un momento concreto. El hook **`commit-msg`** salta justo cuando vas a commitear: revisa el mensaje y, si no cumple, **cancela el commit**. Es un guardián que no se olvida nunca.

> [!warning] Los hooks son LOCALES
> Los hooks viven en `.git/hooks/` y **no se suben a GitHub**. Cada persona los instala en su equipo (o se usan herramientas como `husky` para compartirlos). Por eso los **documentamos** en la bóveda.

> [!example] Vocabulario de esta práctica
> - **Hook:** script que Git ejecuta automáticamente en un evento.
> - **`commit-msg`:** hook que valida el mensaje del commit.
> - **`exit 1`:** el script termina con error → Git cancela el commit.

---

## 📹 Grabación de esta práctica

> [!important] Obligaciones de grabación (LÉEME — es igual en TODAS las prácticas del curso)
> Esta práctica se **graba entera con OBS**, de principio a fin. No es un repaso al final: quiero ver **cómo lo haces tú**.
> 1. **Prepárate primero** (Paso 0): léete el ejercicio entero y ten a mano OBS y tu identificación.
> 2. **Arranca OBS y, nada más empezar, PRESÉNTATE:** *"Hola, me llamo [Nombre], soy alumno de 2.º SMR, y en este vídeo voy a hacer la práctica B0.G.4.2.5 — Marko automatiza con un hook commit-msg (validación de formato)."* Y **muestra en pantalla algo que demuestre que eres tú** (tu **Teams** o tu correo `@alu.edu.gva.es`; cuando tengas GitHub, tu perfil). Di **qué vas a hacer**.
> 3. **Graba todos los pasos** sin cortes, hablando lo que haces.
> 4. **Timestamps SIEMPRE** en la descripción del vídeo: `00:00 Presentación` y **uno por cada paso** (`mm:ss`).
> 5. **Al terminar:** nombra el vídeo **`B0.G.4.2.5 · Marko automatiza con un hook commit-msg (validación de formato)`** y súbelo a tu playlist de YouTube **`B0_Curso_Git`** (como **"No listado"**).
> 6. **Una sola entrega:** este curso **no se duplica casa/centro** (a diferencia de la Fase 0). Grabas y subes el vídeo **una vez**.

---

## 🛠️ Procedimiento
Dentro de `Manuales/`.

> [!example] Paso 0 — Prepárate, ABRE TUS APUNTES y empieza a grabar
> 1. **Léete el ejercicio entero** antes de tocar nada, para no perderte a mitad del vídeo.
> 2. ✍️ **Abre tu entrada de apuntes**, vacía y con la estructura puesta, **antes de empezar**:
>    ```
>    00_Apuntes/Trimestre_1/B0_Curso_Git/git-4.2.5-automatiza-con-un-hook-commit-msg.md
>    ```
>    La plantilla y qué va dentro, en **[📦 Entregables](../02_ENTREGABLES.md)**. **Se abre ahora, no al terminar.**
> 3. **Ten a mano OBS** y una pestaña del navegador con tu **Teams** o tu correo `@alu.edu.gva.es` (para presentarte).
> 4. **Arranca OBS** y pulsa **"Iniciar grabación"**. A partir de aquí, **todo queda grabado**.
> 5. **Preséntate** (mira la caja de grabación de arriba): di tu nombre, muestra tu identidad y di qué vas a hacer.

> [!example] Paso 1 — Entra en la carpeta de hooks
> *🐧 Git Bash / Linux*
> ```bash
> cd .git/hooks
> ```
> *🪟 PowerShell*
> ```powershell
> cd .git\hooks
> ```

> [!example] Paso 2 — Crea el archivo `commit-msg` (sin extensión) **y hazlo ejecutable**
> *🐧 Git Bash / Linux*
> ```bash
> touch commit-msg
> chmod +x commit-msg
> ls -l commit-msg          # tiene que poner -rwxr-xr-x
> ```
> *🪟 PowerShell*
> ```powershell
> New-Item commit-msg -ItemType File
> ```
>
> > [!danger] 🛑 El `chmod +x` no es un adorno: sin él, el hook NO SE EJECUTA
> > En **Linux y Git Bash**, Git solo lanza un hook si el fichero tiene el **bit de ejecución**. Sin `chmod +x` queda en `-rw-r--r--`, Git lo **ignora en silencio**, y tú creerías que funciona porque no da error.
> >
> > Eso es lo peor que le puede pasar a un control de calidad: **que parezca que está y no esté.** La prueba del Paso 4 te lo va a decir.
> >
> > *(En **Windows con PowerShell** no hace falta: ahí Git lanza el hook por su nombre, no por el bit. Por eso el mismo curso se comporta distinto según la máquina — y por eso conviene que lo sepas.)*

> [!example] Paso 3 — Escribe la validación
> Edita `commit-msg` con este contenido:
> ```sh
> #!/bin/sh
> # Valida que el mensaje empieza por EJ-NN-NN-NN o Merge/Revert/Initial
> msg=$(cat "$1")
> if echo "$msg" | grep -qE "^(EJ-[0-9]{2}-[0-9]{2}-[0-9]{2}|Merge|Revert|Initial)"; then
>   exit 0
> else
>   echo "ERROR: el mensaje debe empezar por EJ-FF-NN-OO o Merge/Revert/Initial."
>   exit 1
> fi
> ```

> [!warning] ⚠️ A partir de ahora el formato es obligatorio en TODOS tus commits locales
> **También en los de prueba.** Si en otro ejercicio te piden *"haz un commit de prueba"*, ya no vale `git commit -m "prueba"` — el hook lo rechaza. Ponle código:
> ```
> git commit -m "EJ-02-03-01 · commit de prueba"
> ```
>
> **Lo que el hook NO ve:** las fusiones que haces con el botón de GitHub (*Squash and merge*, *Rebase and merge*). Esos mensajes los genera el servidor, y ahí no llega tu hook. Si luego traes esos commits a local y los reescribes, **respeta tú el formato**.

> [!example] Paso 4 — Pruébalo
> - Intenta commitear con mensaje `"prueba"` → **falla**.
> - Intenta con `"EJ-04-02-05 · prueba"` → **pasa**.

> [!example] Paso 5 — Cierra la grabación y súbela
> 1. **Detén la grabación** en OBS y localiza el archivo del vídeo.
> 2. **Súbelo a YouTube**, a tu playlist **`B0_Curso_Git`**, como **"No listado"**.
> 3. Nombra el vídeo **`B0.G.4.2.5 · Marko automatiza con un hook commit-msg (validación de formato)`**.
> 4. En la **descripción**, añade los **timestamps**: `00:00 Presentación` y **uno por cada paso**.

---

## 🚩 Errores y verificación

> [!warning] Errores típicos (evítalos)
> | Error | Qué pasa | Cómo evitarlo |
> | :--- | :--- | :--- |
> | Le pones extensión (`commit-msg.txt`) | Git no lo ejecuta | El archivo se llama **`commit-msg`**, sin extensión. |
> | Esperas que el hook viaje a GitHub | Los hooks son locales | Documenta el hook; instálalo en cada equipo. |

> [!success] ✅ Verificación: ¿está bien hecho?
> - Un commit con mensaje **sin** formato es **rechazado**.
> - Un commit con el formato correcto **pasa**.

> [!question] Comprueba que lo has entendido
> Responde en tu bitácora:
> 1. ¿Qué es un hook y cuándo se ejecuta el `commit-msg`?
> 2. ¿Qué provoca `exit 1` en el hook?
> 3. ¿Por qué los hooks no llegan a GitHub?

---

## ✅ Entregables y cierre

- **Entregable apuntes:** `git-4.2.5-automatiza-con-un-hook-commit-msg.md` en `00_Apuntes/Trimestre_1/B0_Curso_Git/`, con el enlace del vídeo dentro, **subida con `git push`**.
- **Entregable vídeo:** `B0.G.4.2.5 · Marko automatiza con un hook commit-msg (validación de formato)` subido a la playlist `B0_Curso_Git` (No listado), con presentación al principio y timestamps. **Una sola entrega** (no se duplica casa/centro).
- **Entregable GitHub:** ninguno (los hooks no se versionan por defecto).
- **Entregable bóveda:** documenta el hook en `Manuales/00_infraestructura/configuracion_git.md`.
- **Criterio de éxito:** el hook rechaza mensajes sin formato y acepta los correctos.

> [!summary] 🎓 Qué has aprendido en este ejercicio
> - Qué es un **hook** y cómo `commit-msg` valida el mensaje automáticamente.
> - A escribir una validación que **cancela** el commit (`exit 1`).
> - Que los hooks son **locales** y hay que documentarlos/distribuirlos.
>
> **Siguiente:** EJ-04-03-01 — Marko hace un rebase interactivo (nivel Sobresaliente).
