---
Ejercicio: EJ-04-03-01
Fase: 04
Nivel: 03
Nivel_nombre: Sobresaliente (N3)
Título: Marko hace rebase interactivo (squash + reword + reorder)
Playlist: Curso Git — Fase 4
Vídeo: F4.3.1 · Marko hace rebase interactivo (squash + reword + reorder)
---

## EJ-04-03-01 — Marko hace un rebase interactivo

> [!abstract] Ficha de la práctica
> ### 📌 `F4.3.1` — Marko hace rebase interactivo (squash + reword + reorder)
> - **Fase 4** (Workflows) · **Nivel 3** (Sobresaliente)
> - **🎬 Playlist:** `Curso Git — Fase 4`
> - **📹 Nombre del vídeo:** `F4.3.1 · Marko hace rebase interactivo (squash + reword + reorder)`

> [!info] Situación
> Marko tiene 6 commits en su rama feature: algunos con typos en el mensaje, otros redundantes. **Antes** de abrir el PR, quiere limpiarlos.

- **🎯 Objetivo real:** manejar `git rebase -i` para **reordenar**, **fusionar (squash)**, **reescribir mensajes (reword)** y **eliminar (drop)** commits.
- **🧩 Problema que resuelve:** un PR con commits sucios ("wip", "asdf", "otra vez") refleja trabajo desorganizado.

---

## 📚 Fundamento

> [!info] Antes de empezar: editar tu historia antes de enseñarla
> El **rebase interactivo** (`git rebase -i`) abre un editor con la lista de tus últimos commits y te deja **decidir qué hacer con cada uno**. Es como montar el "corte final" de un vídeo antes de publicarlo: quitas tomas malas, juntas escenas, corriges títulos.

> [!danger] Solo sobre commits que NO has compartido
> Igual que el rebase normal, reescribe la historia (cambia hashes). Hazlo **solo** con commits que aún viven en tu rama y no ha usado nadie.

> [!example] Vocabulario de esta práctica
> - **`pick`:** deja el commit tal cual.
> - **`reword` (r):** conserva el cambio, reescribe el **mensaje**.
> - **`squash` (s):** funde el commit con el anterior.
> - **`drop` (d):** elimina el commit.

---

## 📹 Grabación de esta práctica

> [!important] Obligaciones de grabación (LÉEME — es igual en TODAS las prácticas del curso)
> Esta práctica se **graba entera con OBS**, de principio a fin. No es un repaso al final: quiero ver **cómo lo haces tú**.
> 1. **Prepárate primero** (Paso 0): léete el ejercicio entero y ten a mano OBS y tu identificación.
> 2. **Arranca OBS y, nada más empezar, PRESÉNTATE:** *"Hola, me llamo [Nombre], soy alumno de 2.º SMR, y en este vídeo voy a hacer la práctica F4.3.1 — Marko hace rebase interactivo (squash + reword + reorder)."* Y **muestra en pantalla algo que demuestre que eres tú** (tu **Teams** o tu correo `@alu.edu.gva.es`; cuando tengas GitHub, tu perfil). Di **qué vas a hacer**.
> 3. **Graba todos los pasos** sin cortes, hablando lo que haces.
> 4. **Timestamps SIEMPRE** en la descripción del vídeo: `00:00 Presentación` y **uno por cada paso** (`mm:ss`).
> 5. **Al terminar:** nombra el vídeo **`F4.3.1 · Marko hace rebase interactivo (squash + reword + reorder)`** y súbelo a tu playlist de YouTube **`Curso Git — Fase 4`** (como **"No listado"**).
> 6. **Una sola entrega:** este curso **no se duplica casa/centro** (a diferencia de la Fase 0). Grabas y subes el vídeo **una vez**.

---

## 🛠️ Procedimiento
En la rama feature, dentro de `Manuales/`.

> [!example] Paso 0 — Prepárate y empieza a grabar
> 1. **Léete el ejercicio entero** antes de tocar nada, para no perderte a mitad del vídeo.
> 2. **Ten a mano OBS** y una pestaña del navegador con tu **Teams** o tu correo `@alu.edu.gva.es` (para presentarte).
> 3. **Arranca OBS** y pulsa **"Iniciar grabación"**. A partir de aquí, **todo queda grabado**.
> 4. **Preséntate** (mira la caja de grabación de arriba): di tu nombre, muestra tu identidad y di qué vas a hacer.

> [!example] Paso 1 — Inicia el rebase interactivo de los últimos 6 commits
> ```
> git rebase -i HEAD~6
> ```

> [!example] Paso 2 — Edita la lista
> Se abre un editor con líneas `pick <hash> mensaje`. Cambia lo que quieras:
> - `pick` → **`r`** para reescribir un mensaje.
> - `pick` → **`s`** para fundir un commit con el de arriba.
> - `pick` → **`d`** para eliminar.
> - **Mueve** líneas para reordenar.

> [!example] Paso 3 — Guarda y cierra
> Git aplica los cambios uno a uno. Si toca reescribir mensajes, se abrirá el editor para cada uno.

> [!example] Paso 4 — Comprueba el resultado
> ```
> git log --oneline
> ```
> El histórico de la rama queda limpio y coherente.

> [!example] Paso 5 — Cierra la grabación y súbela
> 1. **Detén la grabación** en OBS y localiza el archivo del vídeo.
> 2. **Súbelo a YouTube**, a tu playlist **`Curso Git — Fase 4`**, como **"No listado"**.
> 3. Nombra el vídeo **`F4.3.1 · Marko hace rebase interactivo (squash + reword + reorder)`**.
> 4. En la **descripción**, añade los **timestamps**: `00:00 Presentación` y **uno por cada paso**.

---

## 🚩 Errores y verificación

> [!danger] No rebases lo ya compartido
> Reescribir commits que otros ya tienen les rompe el histórico. Interactivo = solo para tu trabajo privado.

> [!warning] Si te lías
> ```
> git rebase --abort
> ```
> Vuelve todo al estado anterior. Y si la rama ya estaba subida, sube con `git push --force-with-lease`.

> [!success] ✅ Verificación: ¿está bien hecho?
> - El histórico de la rama es **coherente y limpio** (sin commits basura ni mensajes con typos).

> [!question] Comprueba que lo has entendido
> Responde en tu bitácora:
> 1. ¿Para qué sirve `git rebase -i`?
> 2. ¿Qué hacen `reword`, `squash` y `drop`?
> 3. ¿Por qué no se hace sobre commits ya compartidos?

---

## ✅ Entregables y cierre

- **Entregable vídeo:** `F4.3.1 · Marko hace rebase interactivo (squash + reword + reorder)` subido a la playlist `Curso Git — Fase 4` (No listado), con presentación al principio y timestamps. **Una sola entrega** (no se duplica casa/centro).
- **Entregable GitHub:** histórico de la rama limpio.
- **Entregable bóveda:** sin cambios materiales.
- **Criterio de éxito:** el histórico de la rama es coherente y limpio.

> [!summary] 🎓 Qué has aprendido en este ejercicio
> - A **limpiar** tu historia antes de un PR con `git rebase -i`.
> - Las acciones **reword / squash / drop / reordenar**.
> - Que es una operación de **historia privada** (nunca sobre lo compartido).
>
> **Siguiente:** EJ-04-03-02 — Marko recupera con la notación `HEAD@{N}` del reflog.
