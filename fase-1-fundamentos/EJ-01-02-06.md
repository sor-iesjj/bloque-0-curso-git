---
Ejercicio: EJ-01-02-06
Fase: 01
Nivel: 02
Nivel_nombre: Notable (N2)
Título: Marko sincroniza cambios remotos (`git pull`)
---

## EJ-01-02-06 — Marko sincroniza cambios remotos (`git pull`)

> [!info] Situación
> Marko ha corregido un typo en su `README.md` directamente en GitHub, desde el navegador. Ahora vuelve al portátil: su copia local **NO** tiene ese cambio.

- **🎯 Objetivo real:** aprender `git pull` para traer al ordenador los cambios que están en el remoto.
- **🧩 Problema que resuelve:** sin `pull`, la copia local y la de GitHub divergen. Cuando Marko intente hacer `push`, GitHub se quejará.

---

## 📚 Fundamento

> [!info] Antes de empezar: el gesto que repetirás toda la vida
> Cuando trabajas en **más de un sitio** (el navegador, el portátil, el equipo de casa…), las copias se desincronizan. La regla de oro: **`git pull` al empezar** (bajar lo último) y **`git push` al terminar** (subir lo tuyo). Pull al llegar, push al irte.

> [!abstract] Idea clave: `pull` = traer y fusionar
> `git pull` hace dos cosas de golpe: **descarga** los cambios nuevos del remoto (`fetch`) y los **fusiona** con tu copia local (`merge`). Por eso, tras un `pull`, tu ordenador queda al día.

> [!example] Vocabulario de esta práctica
> - **`git pull`:** baja del remoto y fusiona en local (= `git fetch` + `git merge`).
> - **`git push`:** sube tus commits al remoto.
> - **Divergir:** que dos copias del repo tengan cambios distintos.

---

## 🛠️ Procedimiento

> [!example] Paso 1 — Haz un cambio en GitHub (desde el navegador)
> En GitHub, abre el `README.md` del repositorio `Manuales`, edítalo y añade la línea: `Mantenido por Marko desde 2026.` Pulsa el botón verde **"Commit changes"**.

> [!example] Paso 2 — Vuelve a la terminal, dentro de `Manuales/`
> (PowerShell o Git Bash, da igual.)

> [!example] Paso 3 — Comprueba que tu copia local NO tiene el cambio
> El comando `cat` funciona en las dos terminales:
> ```
> cat README.md
> ```
> La línea nueva **no** aparece todavía.

> [!example] Paso 4 — Trae el cambio
> ```
> git pull
> ```

> [!example] Paso 5 — Verifica que ya está
> ```
> cat README.md
> git log --oneline
> ```
> Ahora sí aparece la línea, y el commit hecho desde la web está en tu historial local.

---

## 🚩 Errores y verificación

> [!warning] Errores típicos (evítalos)
> | Error | Qué pasa | Cómo evitarlo |
> | :--- | :--- | :--- |
> | Editas en local sin hacer `pull` antes | Puede aparecer un **conflicto** | Haz siempre `pull` al empezar. Los conflictos se ven en EJ-02-02-04. |
> | Haces `push` y GitHub lo rechaza | El remoto tenía cambios que no tenías | Haz `git pull` y luego `push`. |

> [!success] ✅ Verificación: ¿está bien hecho?
> - `cat README.md` en local incluye la línea añadida desde la web.
> - `git log --oneline` muestra el commit hecho desde GitHub.

> [!question] Comprueba que lo has entendido
> Responde en tu bitácora:
> 1. ¿Qué dos cosas hace `git pull` por dentro?
> 2. ¿Cuál es la regla de oro al trabajar en varios equipos?
> 3. ¿Por qué puede fallar un `git push` y cómo se arregla?

---

## ✅ Entregables y cierre

- **Entregable GitHub:** el commit hecho desde la web, visible.
- **Entregable bóveda:** `README.md` local actualizado.
- **Criterio de éxito:** la línea añadida en la web aparece en tu copia local tras el `pull`.

> [!summary] 🎓 Qué has aprendido en este ejercicio
> - A **traer cambios** del remoto con `git pull` (que es `fetch` + `merge`).
> - La **regla de oro**: `pull` al empezar, `push` al terminar.
> - Por qué un `push` puede ser rechazado y cómo resolverlo.
>
> **Siguiente:** EJ-01-03-01 — Marko enmienda su último commit (nivel Sobresaliente).
